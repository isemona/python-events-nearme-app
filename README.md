# python-events-nearme-app
A python web-app that uses Meetup.com to alert users about upcoming python-events. 

# Installation

* `virtualenv -p python3 env`
* `source env/bin/activate`
* `pip install -r requirements.txt`

Now, run the app,

* `python server.py`
